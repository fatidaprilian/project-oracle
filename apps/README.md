# Apps

Application boundaries for Project Oracle:

- `frontend/` - user-facing dashboard boundary for Vercel deployment

The current implementation lives in `web/` and can be deployed separately as the frontend app.
