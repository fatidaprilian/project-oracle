# Frontend App

This folder represents the frontend application boundary for Project Oracle.

Current implementation lives in `web/` and is intended for Vercel deployment.

Example:
```bash
cd web
npm install
npm run dev
```
